name: Pipeline CI/CD - DevOps

# Definir gatilhos para execução do workflow
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# Definir variáveis de ambiente para autenticação com a Vercel
env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

# Definir os jobs do workflow
jobs:
  test:
    name: Verificar arquivos do projeto
    runs-on: ubuntu-latest

    # Definir os passos do job de teste
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Verificar arquivos obrigatórios
        run: |
          test -f index.html
          test -f style.css
          test -f script.js

      - name: Verificar sintaxe do JavaScript
        run: node --check script.js

  # Definir o job de deploy, que depende do job de teste
  deploy:
    name: Deploy na Vercel
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/master'

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Instalar Vercel CLI
        run: npm install --global vercel@latest

      - name: Baixar configurações da Vercel
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Fazer build do projeto
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Publicar na Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}